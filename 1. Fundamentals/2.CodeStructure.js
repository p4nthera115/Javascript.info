const header = document.querySelector("h1");
header.innerText = "Code Structure";

// Semicolons imply newline

// This is a comment ( shortcut = Ctrl + / )
/* This is a multiline comment ( shortcut = Ctrl + / ) */

// Prettier formatting = Shift + Alt + F
